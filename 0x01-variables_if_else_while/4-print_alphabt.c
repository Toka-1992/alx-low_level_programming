#include <stdio.h>
/**
* main - print if alphapet lower case except e and q
*
* Description: using the main function
* this program prints "all alphapets in lowercase exept e and q
* Return: 0
*/
int main(void)
{
char ch;
for (ch = 'a' ; ch <= 'z' ; ch++)
{
if (ch != 'e' && ch != 'q')
{
putchar
}
}
putchar('\n');
return (0);
}
